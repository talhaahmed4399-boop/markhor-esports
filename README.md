# Markhor Esports V2
Upload these files to GitHub: index.html, style.css, config.js, app.js.
Before using login/signup, open config.js and replace the Project URL and Publishable/anon key.
Use the base Project URL without /rest/v1/.
Never use a service_role/secret key in config.js.
